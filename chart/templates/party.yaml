{{- $image := printf "%s%s" .Values.registry .Values.party.image -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-party
spec:
  replicas: {{ .Values.party.replicas }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-party
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-party
    spec:
    {{- if .Values.imagePullSecrets }}
      imagePullSecrets:
{{ toYaml .Values.imagePullSecrets | indent 10 }}
    {{- end }}
      containers:
      - name: party
        imagePullPolicy: {{ .Values.party.imagePullPolicy }}
        image: {{ $image }}
    {{- if .Values.party.resources }}
        resources:
{{ toYaml .Values.party.resources | indent 10 }}
    {{- end }}
        command:
          - dorch-party
          - server
          - --internal-port
          - "80"
          - --public-port
          - "3000"
        ports:
        - containerPort: 80
          protocol: TCP
          name: internal
        - containerPort: 3000
          protocol: TCP
          name: public
{{- if .Values.prometheus.enabled }}
        - containerPort: 2112
          protocol: TCP
          name: metrics
{{- end }}
        readinessProbe:
          httpGet:
            path: /readyz
            port: 80
        livenessProbe:
          httpGet:
            path: /healthz
            port: 80
        env:
        - name: SERVICE_NAME
          value: party
        - name: RUST_LOG
          value: info,aws_smithy_runtime=debug,aws_smithy_http_tower=debug,hyper=info
        - name: LOG_LEVEL
          value: {{ .Values.party.logLevel }}
{{ include "dorch.keycloak-env" . | indent 8 }}
{{ include "dorch.metrics-env" . | indent 8 }}
{{ include "dorch.nats-env" . | indent 8 }}
{{ include "dorch.redis-env" . | indent 8 }}
---
{{- if .Values.prometheus.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: {{ .Release.Name }}-party
  labels:
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
spec:
  selector:
    matchLabels:
      app: {{ .Release.Name }}-party
  podMetricsEndpoints:
  - port: metrics
{{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-party
  labels:
    app: {{ .Release.Name }}-party
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}-party
  ports:
  - name: internal
    port: 80
  - name: public
    port: 3000
